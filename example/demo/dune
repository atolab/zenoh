(executable
  (name       demo_sub)
  (modules    demo_sub)
  (libraries  zenoh-ocaml)
  (preprocess (pps lwt_ppx)))

(executable
  (name       demo_pub)
  (modules    demo_pub)
  (libraries  zenoh-ocaml)
  (preprocess (pps lwt_ppx)))
