(executable
  (name       zenohc)
  (modules    zenohc)
  (libraries  str lwt lwt.unix zenoh-proto apero-core apero-net atdgen logs logs.fmt logs.cli fmt fmt.cli fmt.tty cmdliner)
  (preprocess (pps lwt_ppx ppx_deriving.show ppx_deriving.ord)))
