(jbuild_version 1)

(executable
 ((name            zenohd)
 (modules (zenohd zconfig_t transport))
  (libraries (lwt lwt.unix zenoh_pervasives zenoh_proto atdgen))
  (preprocess
    (pps (lwt.ppx ppx_monadic)))))

(executable
  ((name           zenohc)
  (modules (zenohc))
  (libraries (lwt lwt.unix zenoh_pervasives zenoh_proto atdgen))
  (preprocess
    (pps (lwt.ppx ppx_monadic)))))
