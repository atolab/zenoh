(jbuild_version 1)

(executable
 ((name            zenohd)
 (modules (zenohd zconfig_t transport session zengine))
  (libraries (lwt lwt.unix apero zenoh_proto atdgen))
  (preprocess
    (pps (lwt.ppx ppx_monadic)))))

(executable
  ((name           zenohc)
  (modules (zenohc))
  (libraries (lwt lwt.unix apero zenoh_proto atdgen))
  (preprocess
    (pps (lwt.ppx ppx_monadic)))))
