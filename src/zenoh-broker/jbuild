(jbuild_version 1)

(executable
 ((name            zenohd)
 (modules (zenohd zconfig_t))
  (libraries (lwt lwt.unix lwt_log zenoh_proto atdgen))
  (preprocess
    (pps (lwt_ppx ppx_monadic)))))

(executable
  ((name           zenohc)
  (modules (zenohc))
  (libraries (lwt lwt.unix lwt_log zenoh_proto apero atdgen))
  (preprocess
    (pps (lwt_ppx ppx_monadic)))))
