(jbuild_version 1)

(executable
 ((name            zenohd)
 (modules (zenohd zconfig_t))
  (libraries (lwt lwt.unix zenoh_proto atdgen))
  (preprocess
    (pps (lwt.ppx ppx_monadic)))))

(executable
  ((name           zenohc)
  (modules (zenohc))
  (libraries (lwt lwt.unix zenoh_proto apero atdgen))
  (preprocess
    (pps (lwt.ppx ppx_monadic)))))
